# ASP.NET Core 2 - WebApi example project

This is only for example proposes and play around with CI's.

## Setup

install [.NET Core SDK](https://www.microsoft.com/net/download/windows) and the run `dotnet restore`